export type SortOrder = "cheapest" | "nearest";

/** The list either ranks what is nearby, or shows only what the driver has saved. */
export type ListMode = "nearby" | "favorites";

/** The same results, seen as rows or as pins on a map. */
export type ViewMode = "list" | "map";
